## IDF Component Manager Manifest File
dependencies:
    ## Required IDF version
    idf:
        version: ">=4.1.0"
    espressif/esp_h264:
        version: "1.0.4"
        rules:
            - if: target in [esp32p4, esp32s3]
    esp-idf-lib/dht:
        version: "^1.1.7"
